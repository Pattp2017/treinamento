(() => {
  let participantes = [];
  let turmaAtual = null;
  function cfg(){ return window.SUPABASE_CONFIG || {}; }
  function configurado(){ return Boolean(cfg().url && cfg().anonKey); }
  async function supabaseFetch(path, options={}){if(!configurado())throw new Error('Supabase ainda não configurado.');const base=cfg().url.replace(/\/$/,'');const resposta=await fetch(base+'/rest/v1/'+path,{...options,headers:{apikey:cfg().anonKey,Authorization:'Bearer '+cfg().anonKey,'Content-Type':'application/json',Accept:'application/json',...(options.headers||{})}});const texto=await resposta.text();if(!resposta.ok)throw new Error(texto||('HTTP '+resposta.status));return texto?JSON.parse(texto):null;}
  function limparCPF(v){return String(v||'').replace(/\D/g,'');}
  function formatarCPF(v){v=limparCPF(v).slice(0,11);if(!v)return'';return v.replace(/(\d{3})(\d)/,'$1.$2').replace(/(\d{3})(\d)/,'$1.$2').replace(/(\d{3})(\d{1,2})$/,'$1-$2');}
  function validarCPF(cpf){cpf=limparCPF(cpf);if(cpf.length!==11||/^(\d)\1{10}$/.test(cpf))return false;let soma=0,resto;for(let i=1;i<=9;i++)soma+=parseInt(cpf.substring(i-1,i),10)*(11-i);resto=(soma*10)%11;if(resto===10||resto===11)resto=0;if(resto!==parseInt(cpf.substring(9,10),10))return false;soma=0;for(let i=1;i<=10;i++)soma+=parseInt(cpf.substring(i-1,i),10)*(12-i);resto=(soma*10)%11;if(resto===10||resto===11)resto=0;return resto===parseInt(cpf.substring(10,11),10);}
  function htmlTurmas(){return `<div class="grid-3"><div class="card"><h3>📚 Dados do Treinamento</h3><label>Empresa<input id="turEmpresa" readonly></label><label>Turma<input id="turCodigo" readonly placeholder="Será gerado ao salvar"></label><label>Treinamento<input id="turTreinamento" readonly></label><label>Carga Horária<input id="turCarga" readonly></label><label>Data Inicial<input id="turDataInicio" type="date" readonly></label><label>Data Final<input id="turDataFim" type="date" readonly></label></div><div class="card"><h3>👨‍🏫 Dados do Instrutor</h3><label>Instrutor<input id="turInstrutor" readonly></label><label>Habilitação<input id="turHabilitacao" placeholder="Será ligada ao cadastro de instrutores"></label><label>Registro Profissional<input id="turRegistro" placeholder="Será ligado ao cadastro de instrutores"></label><div id="turStatus" class="aviso">Turma ainda não salva.</div></div></div><div class="card"><h3 id="tituloParticipantes">👥 Participantes (0)</h3><div class="grid-3"><label>Nome do Participante<input id="turNome" placeholder="Digite o nome"></label><label>CPF<input id="turCPF" maxlength="14" placeholder="000.000.000-00"></label><div style="display:flex;align-items:end"><button class="btn primario" id="btnAddParticipante">➕ Adicionar</button></div></div><div style="margin-top:14px"><textarea id="turImportacao" rows="5" style="width:100%" placeholder="Importação rápida: Nome;CPF, um por linha"></textarea></div><div style="margin-top:8px"><button class="btn" id="btnImportarParticipantes">📥 Importar participantes</button></div><div style="overflow-x:auto;margin-top:14px"><table style="width:100%;border-collapse:collapse"><thead><tr><th style="text-align:left">Nome</th><th style="text-align:left">CPF</th><th>Ação</th></tr></thead><tbody id="turListaParticipantes"></tbody></table></div></div><div class="card"><div class="acoes"><button class="btn primario" id="btnSalvarTurma">💾 Salvar Turma</button></div><div id="turOrigem" style="margin-top:14px;padding-top:9px;border-top:1px solid #e5e7eb;text-align:right;color:#8a9299;font-size:11px"></div></div>`;}
  function atualizarTabela(){const tbody=document.getElementById('turListaParticipantes');if(!tbody)return;tbody.innerHTML='';participantes.forEach((p,i)=>{const tr=document.createElement('tr');tr.innerHTML=`<td style="padding:8px 4px">${p.nome}</td><td style="padding:8px 4px">${p.cpf}</td><td style="text-align:center"></td>`;const b=document.createElement('button');b.className='btn';b.textContent='🗑️';b.title='Remover participante';b.onclick=()=>{participantes.splice(i,1);atualizarTabela();};tr.children[2].appendChild(b);tbody.appendChild(tr);});document.getElementById('tituloParticipantes').textContent='👥 Participantes ('+participantes.length+')';}
  function adicionarParticipante(nome,cpf){nome=String(nome||'').trim().toUpperCase();cpf=limparCPF(cpf);if(!nome||!cpf)throw new Error('Informe nome e CPF.');if(!validarCPF(cpf))throw new Error('CPF inválido.');if(participantes.some(p=>limparCPF(p.cpf)===cpf))throw new Error('Este CPF já foi adicionado.');participantes.push({nome,cpf:formatarCPF(cpf)});atualizarTabela();}
  function preencherAgendaSelecionada(){const ev=window.TREINAMENTO_AGENDA_SELECIONADA;if(!ev)return;document.getElementById('turEmpresa').value=ev.empresa||'';document.getElementById('turTreinamento').value=ev.treinamento||'';document.getElementById('turCarga').value=ev.carga_horaria?ev.carga_horaria+' horas':'';document.getElementById('turDataInicio').value=ev.data_inicio||'';document.getElementById('turDataFim').value=ev.data_fim||'';document.getElementById('turInstrutor').value=ev.instrutor||'';document.getElementById('turOrigem').textContent='🔗 Origem: '+(ev.codigo||ev.id||'agendamento selecionado');}
  async function carregarTurmaExistente(){const ev=window.TREINAMENTO_AGENDA_SELECIONADA;if(!ev?.id_turma)return;const turmas=await supabaseFetch('turmas?id=eq.'+encodeURIComponent(ev.id_turma)+'&select=*');if(!turmas?.length)return;turmaAtual=turmas[0];document.getElementById('turCodigo').value=turmaAtual.codigo||'';document.getElementById('turHabilitacao').value=turmaAtual.habilitacao_instrutor||'';document.getElementById('turRegistro').value=turmaAtual.registro_instrutor||'';document.getElementById('turStatus').textContent='Turma salva.';const ps=await supabaseFetch('turma_participantes?turma_id=eq.'+encodeURIComponent(turmaAtual.id)+'&select=nome,cpf&order=nome.asc');participantes=(ps||[]).map(p=>({nome:p.nome,cpf:formatarCPF(p.cpf)}));atualizarTabela();}
  async function salvarTurma(){const ev=window.TREINAMENTO_AGENDA_SELECIONADA;if(!ev){alert('Abra a turma a partir de um agendamento.');return;}if(!participantes.length){alert('Adicione pelo menos um participante.');return;}try{const agora=new Date().toISOString();if(!turmaAtual){const codigo='T'+new Date().getFullYear()+String(Date.now()).slice(-5);const payload={codigo,agenda_id:ev.id||null,empresa:ev.empresa||'',treinamento_id:ev.treinamento_id||null,treinamento:ev.treinamento||'',norma:ev.norma||null,carga_horaria:ev.carga_horaria||null,data_inicio:ev.data_inicio,data_fim:ev.data_fim,instrutor:ev.instrutor||'',habilitacao_instrutor:document.getElementById('turHabilitacao').value.trim()||null,registro_instrutor:document.getElementById('turRegistro').value.trim()||null,status:'Aberta',atualizado_em:agora};const resp=await supabaseFetch('turmas',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify(payload)});turmaAtual=resp?.[0];}else{await supabaseFetch('turmas?id=eq.'+encodeURIComponent(turmaAtual.id),{method:'PATCH',body:JSON.stringify({habilitacao_instrutor:document.getElementById('turHabilitacao').value.trim()||null,registro_instrutor:document.getElementById('turRegistro').value.trim()||null,atualizado_em:agora})});await supabaseFetch('turma_participantes?turma_id=eq.'+encodeURIComponent(turmaAtual.id),{method:'DELETE'});}const linhas=participantes.map(p=>({turma_id:turmaAtual.id,nome:p.nome,cpf:limparCPF(p.cpf)}));await supabaseFetch('turma_participantes',{method:'POST',headers:{Prefer:'return=minimal'},body:JSON.stringify(linhas)});await supabaseFetch('agenda?'+(ev.id?'id=eq.'+encodeURIComponent(ev.id):'codigo=eq.'+encodeURIComponent(ev.codigo)),{method:'PATCH',body:JSON.stringify({id_turma:turmaAtual.id,etapa:2,atualizado_em:agora})});ev.id_turma=turmaAtual.id;ev.etapa=2;document.getElementById('turCodigo').value=turmaAtual.codigo||'';document.getElementById('turStatus').textContent='Turma salva com sucesso.';alert('Turma salva com sucesso.');}catch(e){alert('Erro ao salvar turma: '+e.message);}}
  window.renderTurmasGithub=async function(){const area=document.getElementById('conteudoPrincipal');area.innerHTML=htmlTurmas();participantes=[];turmaAtual=null;atualizarTabela();preencherAgendaSelecionada();document.getElementById('turCPF').oninput=e=>{e.target.value=formatarCPF(e.target.value);};document.getElementById('btnAddParticipante').onclick=()=>{try{adicionarParticipante(document.getElementById('turNome').value,document.getElementById('turCPF').value);document.getElementById('turNome').value='';document.getElementById('turCPF').value='';}catch(e){alert(e.message);}};document.getElementById('btnImportarParticipantes').onclick=()=>{const linhas=document.getElementById('turImportacao').value.split(/\r?\n/).filter(Boolean);try{linhas.forEach(l=>{const p=l.split(/[;\t,]/);if(p.length>=2)adicionarParticipante(p[0],p[1]);});document.getElementById('turImportacao').value='';}catch(e){alert(e.message);}};document.getElementById('btnSalvarTurma').onclick=salvarTurma;if(configurado()){try{await carregarTurmaExistente();}catch(e){document.getElementById('turStatus').textContent='Erro ao carregar turma: '+e.message;}}};
})();